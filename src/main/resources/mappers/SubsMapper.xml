<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thehyundai.thepet.domain.subscription.SubsMapper">

    <insert id="saveCurationSubscription" useGeneratedKeys="true" parameterType="SubscriptionVO">
        <selectKey keyProperty="id" resultType="String" order="AFTER">
            SELECT SUBSCRIPTION_SEQ.currval From DUAL
        </selectKey>
        INSERT INTO SUBSCRIPTION (START_DATE, END_DATE, MEMBER_ID, CURATION_ID, PRODUCT_ID, CURATION_YN)
        VALUES (#{startDate}, #{endDate}, #{memberId}, #{curationId}, #{productId}, #{curationYn})
    </insert>

    <insert id="saveProductSubscription" useGeneratedKeys="true" parameterType="SubscriptionVO">
        <selectKey keyProperty="id" resultType="String" order="AFTER">
            SELECT SUBSCRIPTION_SEQ.currval From DUAL
        </selectKey>
        INSERT INTO SUBSCRIPTION (START_DATE, END_DATE, MEMBER_ID, CURATION_ID, PRODUCT_ID, CURATION_YN)
        VALUES (#{startDate}, #{endDate}, #{memberId}, #{curationId}, #{productId}, #{curationYn})
    </insert>

</mapper>